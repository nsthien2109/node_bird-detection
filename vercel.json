{
  "version": 2,
  "builds": [
    {
      "src": ".",
      "use": "@vercel/node",
      "config": {
        "command": "npm run start"
      }
    }
  ],
  "routes": [
    {
      "src": "/api(.*)",
      "dest": "/api/:path*$1"
    }
  ]
}
